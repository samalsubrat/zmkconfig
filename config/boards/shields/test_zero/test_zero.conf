# Enable ZMK display
CONFIG_ZMK_DISPLAY=y

# Enable SSD1306 driver
CONFIG_DISPLAY=y
CONFIG_SSD1306=y

# Enable LVGL (for ZMK’s status screen)
CONFIG_LVGL=y
CONFIG_LVGL_USE_GPU=n
CONFIG_LV_COLOR_DEPTH_1=y
CONFIG_LV_COLOR_SCREEN_TRANSP=n

# Logging (optional)
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3
